---
# not ansible_check_mode: в сухом прогоне на свежем сервере юнита ещё нет.
- name: Restart xray-entry
  ansible.builtin.systemd_service:
    name: xray-entry
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode

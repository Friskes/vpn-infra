# Ставится в локальный venv: make install
# 2.17 — потолок для Python 3.10 (Ubuntu 22.04 / WSL), версии коллекций подобраны под него.
ansible-core==2.17.14
pre-commit==4.5.1
# ansible-lint сюда не ставится: он требует ansible-core != 2.17.* и при установке
# ломает закреплённую выше версию. Он живёт в своём окружении pre-commit.

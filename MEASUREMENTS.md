# Полевые замеры

Результаты, а не инструкции: что показали конкретные резолверы у конкретных операторов в
конкретный день. Инструкции живут в [TUNNEL.md](TUNNEL.md) и [PROXY.md](PROXY.md), инструменты —
в [MEASURE.md](MEASURE.md).

**Эти цифры устаревают.** Порядок резолверов переворачивается за пару недель, оператор
меняет правила без предупреждения. Ценность файла не в самих числах, а в том, какие выводы
из них уже сделаны — чтобы не проверять по третьему разу то, что проверено.

## МТС, юг России — 19 августа 2026

Белые списки активны: белый `ya.ru` открывается за 0,25 с, небелый адрес — таймаут, свой
сервер — 100% потерь.

| Резолвер | Через туннель | Что с ним не так |
|---|---|---|
| `213.87.74.21` | **154–215 КБ/с** | рабочий: рекурсия идёт через `213.87.74.54`, ответ полный |
| `213.87.0.1` | 25 КБ/с | его рекурсор режет ответ до 224–369 Б вместо 1047 |
| `95.153.131.2` | 4–21 КБ/с | клиент уходит в прямой режим, см. ниже |

Живых резолверов нашлось 12 из 522 проверенных — **ровно столько же, сколько в свободном
режиме несколькими часами раньше**: ограничения на сами резолверы оператора не влияют, они
в белом списке по определению. Кроме перечисленных отвечают `95.153.135.2`, `213.87.1.1`,
`213.87.74.5`, `213.87.74.64`, `213.87.99.100`, `213.87.210.20`, `213.87.211.20`.

> ⚠️ **Резолвер может пройти все проверки и всё равно не работать.** `95.153.131.2` — это
> DNS, который оператор выдаёт телефону по DHCP, и SlipNet с ним уходит в прямой режим: на
> сервер прилетают пакеты с абонентского адреса (`91.78.x`), а не от рекурсора оператора.
> Прямой путь белый список режет — отсюда 4–21 КБ/с с обрывами. Проверено дважды, с полным
> переподключением.
>
> На уровне DNS такие резолверы неотличимы: ответы совпадают байт в байт, длинные имена и
> типы TXT/NULL/CNAME/AAAA обрабатываются одинаково. Разница только в том, как их трактует
> клиент, — поймать это можно лишь замером скорости через сам туннель.
>
> Если замер подозрительно низкий, посмотреть на сервере
> `tcpdump -ni any 'udp port 53 and dst host <IP сервера>'`: источником должен быть адрес
> оператора (`213.87.x`, `95.153.x`), а не абонентский `91.78.x`.

Двумя неделями раньше расклад был другим: **порядок переворачивается, разовый выбор
устаревает.**

## Мегафон, юг России — 19 августа 2026, вечер

Белые списки активны. **Slipstream там не работает** — 0–0,3 КБ/с при любых резолверах и
настройках. Замер с двух сторон показывает, где теряется:

| Что мерили | Результат |
|---|---|
| сервер отдал за 18 с | 359 КБ, 343 ответа по 1047 Б |
| рекурсор переспросил | 0% — до него дошло всё |
| телефон получил за 28 с | 9,8 КБ |

Пропадает участок рекурсор → телефон, потери там 17–80% и **растут по ходу сессии**: в
21:44 крупные ответы доходили 3 раза из 3, к 22:20 — 42%. От размера ответа это не зависит,
то есть режется сам поток. Прямой путь к серверу (Authoritative Mode) днём проходил, к
вечеру закрылся полностью — 0 из 420 проб.

Что помогло и что нет:

- **DCUBIC вместо BBR** — единственная настройка, давшая эффект: сервер перестал
  захлёбываться, ответы выросли с 209 до 1047 Б, отдача с 0,2 до 20 КБ/с. До телефона это
  всё равно не доезжает, но для канала с потерями dcubic правильнее — его же советует
  upstream для рекурсивного режима. Выдать такой профиль:
  `slipnet-user link <имя> <IP> dcubic`.
- **Список резолверов** (fanout по четырём) — не помогает, 0,0 КБ/с.
- **Урезание EDNS до 512** — миф: рекурсоры Мегафона просят 1232, ответы по 1141 Б доходят.
  Бейдж `EDNS✓(1232)` в сканере приложения ничего не измеряет — он лишь проверяет, что в
  ответе есть OPT-запись.

Живых резолверов 9 из 269: `195.208.5.1`, `195.208.4.1` (НСДИ), `10.93.233.228`,
`10.93.233.204`, `10.93.233.236`, `10.161.158.250`. **`10.93.233.196` — тот, что телефон
получает по DHCP, — под нагрузкой мёртв** (0 зап/с, 100% потерь), и клиент с ним сваливается
в прямой режим.

Вывод: Мегафону нужен второй туннель — dnstt, см. [TUNNEL.md](TUNNEL.md).

## Мегафон: что даёт dnstt

Тот же вечер, ограничения в самой жёсткой за день фазе:

| Туннель и транспорт | Скорость |
|---|---|
| Slipstream, UDP | 0,3 КБ/с |
| dnstt, DoT через `77.88.8.8:853` | 1,7 КБ/с |
| dnstt, TCP через `10.93.233.228:53` | **2,8 КБ/с** |

Порядок величины изменился, но для сайтов этого мало. Не пробовано: DoH (у Яндекса по
`/dns-query` редирект, остальные провайдеры закрыты), тумблер Auto-tune, замер днём.
Тумблер **Authoritative Mode в профиле dnstt включать нельзя** — с ним запросы вообще
перестают доходить до сервера.

Незакрытая зацепка: TCP через Яндекс давал 40 запросов в секунду против 15 через резолвер
Мегафона — канал там втрое шире, но сессия SOCKS не встала ни разу из двух. Если браться за
Мегафон снова, начинать стоит отсюда.

## Мелочи, экономящие время

У МТС нет перехвата UDP/53; `8.8.8.8` недоступен даже без ограничений; НСДИ (`195.208.4.1`,
`195.208.5.1`) на МТС молчит, а на Мегафоне работает; `213.87.74.50` и `.58` отвечают
снаружи, но абоненту МТС нет; на Android 16 системные DNS видны только через
`adb shell dumpsys connectivity`.

Под белым списком Мегафона открыты TCP и TLS до резолверов: `77.88.8.8:853` (DoT, 124 мс),
`10.93.233.228:53` по TCP (82 мс), `77.88.8.8:53` (82 мс). Закрыты Cloudflare DoT, Google
DoH и прямой путь к своему серверу — как по IPv4, так и по IPv6.
